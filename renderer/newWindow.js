// newWindow.js

document.addEventListener('DOMContentLoaded', function () {
    // This function will be executed once the DOM is fully loaded

    // Example: Change the content of a paragraph in the new window
    const paragraph = document.getElementById('newWindowParagraph');
    paragraph.textContent = 'This content was updated by newWindow.js';
});
